# Machine-local Claude Code permissions, not shared
.claude/settings.local.json

# OS / editor noise
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store
*.swp
*~

# Note: amitdusane-site-complete/.gitignore handles Hugo build output
# (public/, .hugo_build.lock, resources/_gen/) relative to its own directory.
